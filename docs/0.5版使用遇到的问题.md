1、菜单要调整为中文
2、按住option开始说话。在语音输入的过程当中，如果停顿了时间有2~3秒。字幕上的文字就会消失。然后开始。输入新的语音流。但是我松开 option 时，所有的文本都会填充进来，这个是正确的。是否可以做到所有的文字都在字幕上显示，不要消失，这样体验感和思路会中断。如果语音输入非常流畅的话，是没有这个问题的。但是有的时候会停下来想一想，这个时候字幕上的文字就会中断，体验感不是很好。能否优化一下。
3、 我把这个程序部署在另外一台机器上面，出现一个问题，就是我最输入了一大段，字幕上面也都正常显示，但是我松开 option 之后，只输出了头三个字，后面的都没有，然后我再次按下 option 键，就看到了完整的内容。
4、摁住 option 键之后，字幕上面会有 listen 的字样。是否能够再提醒一下。目前光标是否聚焦在输入框上？这样可以避免讲了一大堆，发现松开后没有输出。
5、讲了一段话之后，松开 option 键，但是只输出了其中一半的文字。然后我再次按下 option 键，就看到了剩余的内容。在实际输入的时候，这两段话中间会有一定的中间隔，但是并不久。可能是一两秒。
6、有的时候完成后收尾中会比较久，大概有个四五秒这样影响体验。是否可以优化一下。